# examples/

Holds `sample_history.csv` (~10-20 rows of historical draw data, header
`date,n1,n2,n3,n4,n5,n6`, per `docs/SDD.md`'s `DATA-IN-100` format) and
a matching sample config — added by `DELIV-900` (run documentation),
once a working end-to-end example exists to capture. Placeholder
directory established now so the path referenced by the root
`README.md` already resolves.
