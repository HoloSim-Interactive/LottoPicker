# Sample lottopicker config (UI-002) -- pairs with sample_history.csv
# in this same directory. See the root README.md's "Config file
# format" / "Launch command" sections for how this is used.

# Resolved relative to this config file's own directory, not the
# process's working directory.
data_file=sample_history.csv

# Retained top-N envelope size -- kept small here so the sample
# console output in the README stays short and easy to eyeball.
top_n=10
